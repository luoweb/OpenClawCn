{
  "file": "src/daemon/schtasks.ts",
  "description": "Windows schtasks 兼容修复 - 命令不可用时不阻断 onboarding",
  "replacements": {
    "throw new Error(`schtasks unavailable: ${detail || \"unknown error\"}`.trim());": "throw new Error(`schtasks 不可用: ${detail || \"未知错误\"}. 在 Windows 上请使用 PowerShell/CMD 运行，或跳过守护进程安装。`.trim());"
  }
}
